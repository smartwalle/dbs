package dbs

import (
	"testing"
)

func TestAND(t *testing.T) {
	//var and = AND(SQL("a=?", 1), SQL("b=?", 2))
	//fmt.Println(and.ToSQL())
	//
	//and = AND(OR(SQL("a=?", 1), SQL("b=?", 2)), OR(SQL("a=?",1), SQL("c=?", 2), IN("ddd", []int{1,2,3,4,5,5})))
	//fmt.Println(and.ToSQL())
	//
	//fmt.Println(SQL("a1=10").ToSQL())
}